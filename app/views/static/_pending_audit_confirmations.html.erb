<% pending_audit_confirmations.each do |pending_audit_confirmation| %>
	<%= link_to "I did not perform any overtime for the week starting #{pending_audit_confirmation.start_date.strftime("%a %b %d")}", root_path, class: 'btn btn-lg btn-primary btn-block', data: { confirm: 'Are you sure you want to confirm you did not perform any overtime?'} %>
<% end %>